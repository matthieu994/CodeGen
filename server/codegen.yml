schema: http://localhost:4000/graphql
generates:
  graphql/types.d.ts:
    plugins:
      - add: "/* eslint-disable */"
      - typescript
    config:
      noExport: true
      scalars:
        Date: Date
